<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.huifangyuan.app.mapper.CommentMapper" >

    <select id="getProductCommentById" resultType="com.huifangyuan.app.vo.CommentVo" parameterType="long">
        SELECT c.*,cr.comment_id,cr.content replayContent,cr.create_time userCreateTime,cr.type from pms_comment c LEFT JOIN pms_comment_replay cr ON c.id = cr.comment_id WHERE product_id =#{productId}
    </select>
    <select id="getShopCommentById" resultType="com.huifangyuan.app.vo.CommentVo" parameterType="long">
        SELECT c.*,cr.comment_id,cr.content replayContent,cr.create_time userCreateTime,cr.type from pms_comment c LEFT JOIN pms_comment_replay cr ON c.id = cr.comment_id WHERE c.shop_id=#{shopId}
    </select>
</mapper>